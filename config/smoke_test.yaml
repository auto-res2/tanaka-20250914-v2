# =============================================================
# Smoke-test configuration – should finish in <2 minutes
# =============================================================
seed_list: [111]
results_dir: .research/iteration27
experiments:
  - id: smoke_run
    models:
      - repo: stabilityai/stable-diffusion-2-1-base  # small-ish, widely available
        resolution: 512
    compression:
      method: vanilla
    generation:
      num_images: 1
      num_inference_steps: 2
      guidance_scale: 7.5
