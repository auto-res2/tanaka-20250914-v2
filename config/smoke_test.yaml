general:
  device: "cpu"
  seed: 42
  num_workers: 0

model:
  name: "google/vit-base-patch16-224"
  revision: "main"

dataset:
  hf_repo: "cifar10"
  split: "test[:100]"
  resolution: [224]
  max_images: 10

experiment_1:
  lambdas: [0.10]
  baselines: ["no_cache"]
  batch_size: 2
