# Full-scale configuration – still lightweight enough for the evaluation server

general:
  device: "cpu"
  seed: 0
  num_workers: 0

dataset:
  hf_repo: "cifar10"
  split: "test[:10]"
  resolution: [224]
  max_images: 10

model:
  name: "google/vit-base-patch16-224"
  revision: "main"

experiment_1:
  lambdas: [0.05, 0.1]
  baselines: ["no_cache"]
  batch_size: 2
